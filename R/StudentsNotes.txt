R Programming March'26

# basic mathematical operations
# addition, subtraction, multiplication, division
6 + 7   # addition
15 - 8  # subtraction
3 * 6   # multiplication
45 / 9  # division
4^3    # exponential
4**3   # exponential

# level of priority for multiple operations

# 1. brackets
# 2. exponential
# 3. division
# 4. multiplication
# 5. addition and subtraction

45 %% 4   #  gives the remainder when 45 is divided by 4
45 %/% 4   #  gives the modulus when 45 is divided by 4

x1 = 8
str(x1)   # returns as num (numeric)

x2 = 9L
str(x2)   # returns as int (integer)

my_name = 'Sudeep Bhargava'   # single quotes can be used
str(my_name)
course_name = "R Programming"  # double quotes can be used
str(course_name)


item1 = 'sudeep\'s laptop'    # override the ' or " coming within the string


k1 = TRUE    # k1 is a logical value 
k2 = F       # k2 is a logical value with the value as FALSE

k1 = c(4,8,7,3,2)    # creates a vector with the name k1

length(k1)    # number of values inside the vector k1
k1[1]    # extracts the first value from vector k1

# counting or indexing in R starts with 1


k1 = append(k1,5)     # inserts the number 5 at the last position of the vector k1

k1 = append(k1,9,3)   # inserts a number 9 after the index # 3. 9 gets the index # 4

k2 = c(7,8,12)
k1 = append(k1,k2)    # the entire vector k2 or all the values of k2 are appended after the last value of k1

k2 = c(5,8,6)
k1 = append(k1,k2,4)   # the values of vector k2 get inserted after index # 4 in vector k1 and k1 gets overwritten

k1[3] = 10    # changes the third value (index # 3) to 10

k1 = k1[-4]    # deletes the value from the index # 4

k1[c(3,6,8)]    # extracts values from 3rd, 6th and 8th index 

k1[c(2,5,7)] = c(4,15,5)  # changes the 2nd, 5th and 7th values to 4,15 and 5 respectively

k1 = k1[-c(3,5)]    # deletes the third and fifth value	

sum(k1)    # adds all values of vector k1
prod(k1)   # gives the product of all values of k1
min(k1)    # minimum value in the vector k1
max(k1)    # maximum value in the vector k1
cumsum(k1)    # cumulative sum upto each index
cummin(k1)    # lowest value of the vector k1 upto the particular index
cummax(k1)    # highest value of the vector k1 upto the particular index

RECYCLING OF VECTORS

x1 = c(5,3,2,7)
x2 = c(1,2,3,4)
x3 = c(6,8)
x4 = c(4,7,3)

# Case-1 : Operating a vector with a scalar quantity
x1 + 5
[1] 10  8  7 12

# Case-2 : Operating two vectors of the same length
x1 + x2
[1]  6  5  5 11

Case-3 : Operating two vectors of different length but length of the longer vector is an exact multiple of length of smaller vector
x1 + x3
[1] 11 11  8 15
x1*x3
[1] 30 24 12 56


Case-4 : Operating two vectors of different length but length of the longer vector is NOT an exact multiple of length of smaller vector

x1 + x4
[1]  9 10  5 11
Warning message:
In x1 + x4 :
  longer object length is not a multiple of shorter object length


Creating special vectors

x1 = 40:70   # creates a vector x1 with consecutive values rom 40 to 70 (including both)

x1 = seq(4,24,3)    # the vector stops at 22. The last value has to be <= 24
x1
[1]  4  7 10 13 16 19 22

x1 = seq(40,8,-3)   # the vector stops at 10. The last value has to be >= 8
x1
[1] 40 37 34 31 28 25 22 19 16 13 10

x1 = seq(4,46,length.out=13)    # initial and final value would be 4 and 46 respectively. Number of values in the vector are fixed to be 13. Gap between two numbers would be decided by R

x1 = rep(c(3,5,7), times = 4)   # the block 3,5,7 repeats 4 times

x1 = rep(c(3,5,7), each = 4)    # each number individually appears 4 times

x1 = rep(c(3,5,7), times = c(4,2,5))  # the number 3 appears 4 times, 5 appears 2 times and 7 appears 5 times

Logical operations


# logical operations
x1 = T
x2 = T
x3 = F
x4 = F
str(x3)
 logi FALSE
# AND operation
x1 & x2   # x1 AND x2
[1] TRUE

x1 & x3   # x1 AND x3
[1] FALSE

# OR operation
x1 | x3
[1] TRUE

x4 | x3
[1] FALSE

# NOT operation
 x1
[1] TRUE
!x1
[1] FALSE


# which command
x1 = c(7,6,4,8,3,5,2,6,9,3,2,7)
length(x1)
[1] 12
# need re-submission of the marks < 4
# Q1 : where are the values OR what are the indices of the values where they are < 4
# Q2 : How many values in vector are < 4
# Q3 : What are the values which < 4

x1 < 4
[1] FALSE FALSE FALSE FALSE  TRUE FALSE  TRUE FALSE FALSE  TRUE  TRUE FALSE
which(x1 < 4)    # Answer to Q1
[1]  5  7 10 11

# the input to the which command is a logical value or vector
# the output of the which command are the indices of the vector where the value is true

length(which(x1 < 4))   # Answer to Q2
[1] 4
x1[which(x1 < 4)]       # Answer to Q3
[1] 3 2 3 2

x1 = c('Bangalore','Pune','Navi Mumbai','Nagpur','Hyderabad','Raipur')
nchar(x1)   # number of characters (including spaces) in each value

grep('pur', x1, value = T)    # returns the values having the pattern pur 
substr(x1,2,7)    # extracts the characters from 2nd to 7th position in each string
gsub('pur','pore',x1)   # substitutes the strings having pur with pore

r1 = 'Sudeep Bhargava'
r1_parts = strsplit(r1," ")[[1]]    # split is applied wherever there is a space
r1_parts
[1] "Sudeep"   "Bhargava"

r1_chrs = strsplit(r1,"")[[1]]     # split is applied at every character
r1_chrs
[1] "S" "u" "d" "e" "e" "p" " " "B" "h" "a" "r" "g" "a" "v" "a"


a1 = c('A','B','C')
a2 = c(1,2,3)

paste(a1,a2)
[1] "A 1" "B 2" "C 3"

paste(a1,a2, sep="-")
[1] "A-1" "B-2" "C-3"

a3 = paste(a1,a2, sep="-", collapse = "***")
> a3
[1] "A-1***B-2***C-3"

# Ways to make matrix
# Option-1
mat1 = matrix(1:24,nrow=3)   # number of values in vector should be a multiple of number of rows
mat1 = matrix(1:24,ncol=6)   # number of values in vector should be a multiple of number of columns

# Option-2
> mat1 = matrix(1:24,nrow=3)
> mat1
     [,1] [,2] [,3] [,4] [,5] [,6] [,7] [,8]
[1,]    1    4    7   10   13   16   19   22
[2,]    2    5    8   11   14   17   20   23
[3,]    3    6    9   12   15   18   21   24

# Number of columns have been decided such that all values of the vector are utilized ATLEAST once.

# Option-3
mat1 = matrix(1:24, nrow=5, ncol=7)
Warning message:
In matrix(1:24, nrow = 5, ncol = 7) :
  data length [24] is not a sub-multiple or multiple of the number of rows [5]
> mat1
     [,1] [,2] [,3] [,4] [,5] [,6] [,7]
[1,]    1    6   11   16   21    2    7
[2,]    2    7   12   17   22    3    8
[3,]    3    8   13   18   23    4    9
[4,]    4    9   14   19   24    5   10
[5,]    5   10   15   20    1    6   11


mat1 = matrix(1:12, nrow=3)
mat1
     [,1] [,2] [,3] [,4]
[1,]    1    4    7   10
[2,]    2    5    8   11
[3,]    3    6    9   12

mat1[2,3]   # extracts the values from 2nd row and 3rd column
[1] 8

mat1[3,2]   # extracts the values from 3rd row and 2nd column
[1] 6

mat1[2,]    # extracts the second row as a vector
[1]  2  5  8 11

mat1[c(1,3),]    # extracts the first and third row as a matrix
     [,1] [,2] [,3] [,4]
[1,]    1    4    7   10
[2,]    3    6    9   12

mat1[,c(2,4)]    # extracts the second and fourth column as a matrix
     [,1] [,2]
[1,]    4   10
[2,]    5   11
[3,]    6   12


mat1 = matrix(1:12, nrow=3)
rownames(mat1) = c('Anjali','Akash','Abhinav')
colnames(mat1) = c('R','AAS','PML','AI')

mat1 = rbind(mat1,r1)		# Adds a new row to the existing matrix
rownames(mat1)[4] = 'Abhishek'


CV =  c(6,9,5,4)
mat1 = cbind(mat1,CV)		# Adds a new column to the existing matrix

nrow(mat1)	# gives the number of rows in a matrix
ncol(mat1)	# gives the number of columns in a matrix





n1 = c('Gayatri','Shikha','Rahul','Chetan')
locn = c('MUM','DLI','BLR','CHN')
m1 = c(68,76,59,92)

stu_df = data.frame(n1,locn,m1)
stu_df = data.frame(Name=n1,Location=locn,Marks=m1)

# Adding rows to existing data frame

new_df = data.frame(n1=c('Sudeep','Rakesh'),k1 = c('BLR','LKO'), m1= c(80,90))
colnames(new_df) = colnames(stu_df)	# name of the columns should match for rbind command used on DFs
stu_df = rbind(stu_df,new_df)


# Adding columns to existing data frame
Marks_AAS = c(70,80,60,55,75,82,91,85)
stu_df = cbind(stu_df,Marks_AAS)


stu_df$Marks_PML   # Extracts the values of column Marks_PML as vector
m2 = c(60,70,50,92,64,58,67,91)
stu_df$Marks_AI = m2 		# Adds a new column with the name Marks_AI and values from vector m2

stu_df[-c(4,5),]	# the fourth and fifth rows are not displayed
stu_df[,-c(2,4)]	# the second and fourth columns are not displayed


library(readxl)
df = read_excel('ERPData.xlsx',sheet = 'Info')
nrow(df) 	# number of rows in df
ncol(df)	# number of columns in df

unique(df$MaterialID)	# the unique values in a particular column of the df

newdf1 = df[which(df$Location=='MWH-2'),]   # creates a subset newdf with the Location as ONLY MWH-2

newdf2 = df[which(df$Location=='MWH-2' & df$MaterialID=='GCVB-79'),]   # creates a subset newdf with the Location as ONLY MWH-2 AND MaterialID as GCVB-79

newdf3 = subset(df, df$Location=='MWH-2' & df$MaterialID=='GCVB-79')   # same as newdf2

sum(newdf2,3)    # gives the total quantity of GCVB-79 at MWH-2



Myfunc = function(a,b){
  x1 = 2*a+b
  print(x1)
}


user_name = readline('Please enter your name ')    # takes the input of a string from the user
user_age = as.numeric(readline('Please enter your age '))	# takes the input of a number from the user

CalcTax=function(){
  salary = as.numeric(readline('Enter your salary '))
  tax=salary*0.1
  print(tax)
}

CalcTax = function(sal,rate=0.1){	# sets a default value for rate as0.1
  tax = sal*rate
  print(tax)
}

MyFunc = function(a,b=3,c=7){
  d = a+b*c
  print(d)	
}

MyFunc(9)	# default values of b and c are used
MyFunc(9,,10)	# default value of b is used but the value c would be 10
MyFunc(9,5)	# default value of c is used but the value b would be 5

# salary below or equal to 20000 => 0 tax
# salary above 20000 => 10% tax

CalcTax=function(salary){
  if (salary<=20000) {
    tax=0
  } else {
    tax = 0.1*salary
  }
  print(tax)
}


# salary below or equal to 20000 => 0 tax
# salary above 20000 and below or equal to 50000 => 10% tax
# salary above 50000 => 15% tax

CalcTax = function(sal){
  if (sal<=20000){
    tax = 0
  } else {
  if (sal<=50000){
    tax = 0.1*sal
  } else {
    tax= 0.15*sal
  } 
  }
  print(tax)
}



sal = as.numeric(readline('Enter salary '))
grade = ifelse(sal<=50000,'A','B')   # if the condition is true than value of grade would be A, if false then B
print(grade)

# if sal<=50000 grade=>A
# if sal>50000  and <=100000 grade=>B
# if sal > 100000 grade=> C

sal = as.numeric(readline('Enter salary '))
grade = ifelse(sal<=50000,'A',ifelse(sal<=100000,'B','C'))
print(grade)


# client is a government company - tax = 5%
# client is a private company - tax = 15%
# client is an individual - tax = 10%

client = readline('Enter the type of client ')
taxrate = switch(client, govt=5, pvt=15, individual=10, 20)
print(taxrate)


# for loop

for (ctr in 2:10){
  print(ctr**2)
}

myvect = c()
for (ctr in 1:3) {
  n = as.numeric(readline('Enter the number '))
  myvect = append(myvect,n)
}
print(myvect)


# condition for the while should be ensured to be true
# the variable used for the entry condition should get changed inside the loop

my_vect = c()
n1 = 7
while(n1 != 0) {
 n1 = as.numeric(readline('Enter the value '))
 my_vect = append(my_vect,n1)
}
print(my_vect)

my_vect = c()
r = 'Y'
while(r != 'N') {
  n1 = as.numeric(readline('Enter the value '))
  my_vect = append(my_vect,n1)
  r = readline('Do you want to continue Y/N ')
}
print(my_vect)























